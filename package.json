{
  "name": "coinrewind",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "fetch-data": "node --env-file=.env scripts/fetch-data.mjs",
    "fetch-spot": "node --env-file=.env scripts/fetch-data.mjs --spot"
  },
  "dependencies": {
    "astro": "^5.0.0"
  }
}
